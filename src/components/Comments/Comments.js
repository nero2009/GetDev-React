import React from 'react';
import PropTypes from 'prop-types';

const Comments = props => {
    return (
        <div>
            {props.children} Comments
        </div>
    );
};



export default Comments;